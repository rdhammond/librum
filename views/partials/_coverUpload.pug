.photo-upload
	.preview
		img#cover.cover.center-block(src=coverUrl)
	.controls.text-center
		input#fileupload.fileupload(type="file")
		button#selectfile.selectfile.btn.btn-default(type="button") Change Cover
		p.help-block Cover changes are not permanent until book is added.
